Ans 1. PyInputPlus is not a part of the Python Standard Library, we must install it separately using Pip.

Ans2.  You can import the module with import pyinputplus as pyip so that you can enter a shorter name when calling the module’s functions.

Ans 3. The difference is in the data-type when you use the first one the program expects an integer value as input but in the latter it expects a float value i.e number containing a decimal

Ans4.  By using pyip.inputint(min=0, max=99).

Ans 5.  A list of regex strings that are either explicitly allowed or denied.
 
Ans 6. The function will raise RetryLimitException.

Ans7. The function returns the value 'hello'.

