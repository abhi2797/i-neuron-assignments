Ans1. Relative paths are relative to the current working directory

Ans2. Absolute paths start with the root folder, such as / or C:\.

Ans3. The os.getcwd() function returns the current working directory. The os.chdir() function changes the current working directory


Ans4. The . folder is the current folder, and .. is the parent folder

ans5. C:\bacon\eggs is the dir name, while spam.txt base name.

ans 6. The string 'r' for read mode 
'w' for write mode and 'a' for append mode.

ans7. An existing file opened in write mode is erased and completely overwritten.

ans 8.  The read() method returns the file’s entire contents as a single string value. The readlines() method returns a list of strings, where each string is a line from the file’s contents.

ans 9.  A shelf value resembles a dictionary value; it has keys and values, along with keys() and values() methods that work similarly to the dictionary methods of the same names.